# GEB Test Scenarios
#
# Each scenario tests whether SKILL.md correctly shapes Claude's behavior.
# Run with: tests/run.sh
#
# Fields:
#   name:            unique identifier
#   skill:           which skill to test (prelude | geb:think | geb:plan | geb:debug | geb:review | geb:groove) — default: prelude
#   category:        depth-signal | upgrade | discipline | conflict | evolution | diagnosis | quality
#   prompt:          what the user says
#   context_files:   files to create in the temp project directory
#   pass_criteria:   what the response SHOULD do (list)
#   fail_patterns:   what the response should NOT contain (list of strings/patterns)

# ============================================================================
# DEPTH SIGNAL — prelude detects ambiguity and engages structured thinking
# ============================================================================

- name: refactor-messy-function
  skill: prelude
  category: depth-signal
  prompt: "This function is getting hard to read, clean it up"
  context_files:
    utils.py: |
      def process_order(order, user, inventory, config):
          if order['status'] == 'pending':
              if user['tier'] == 'premium':
                  discount = config['premium_discount']
                  if order['total'] > config['free_shipping_threshold']:
                      shipping = 0
                  else:
                      shipping = config['shipping_cost'] * 0.5
              else:
                  discount = 0
                  if order['total'] > config['free_shipping_threshold']:
                      shipping = 0
                  else:
                      shipping = config['shipping_cost']
              for item in order['items']:
                  if item['id'] not in inventory:
                      return {'error': 'out_of_stock', 'item': item['id']}
                  if inventory[item['id']] < item['quantity']:
                      return {'error': 'insufficient_stock', 'item': item['id']}
              total = order['total'] * (1 - discount) + shipping
              return {'status': 'confirmed', 'total': total}
          elif order['status'] == 'confirmed':
              return {'error': 'already_confirmed'}
          else:
              return {'error': 'invalid_status'}
  pass_criteria:
    - Identifies this as a task with some complexity worth acknowledging
    - Actually performs the refactoring
    - Does not write a multi-paragraph analysis before touching code
  fail_patterns:
    - Writes 3+ paragraphs before touching code

- name: fix-vague-bug
  skill: prelude
  category: depth-signal
  prompt: "The login isn't working properly, can you look into it?"
  context_files:
    auth.py: |
      import hashlib

      def login(username: str, password: str) -> bool:
          user = db.find_user(username)
          if user is None:
              return False
          hashed = hashlib.md5(password.encode()).hexdigest()
          return hashed == user['password_hash']

      def create_user(username: str, password: str):
          hashed = hashlib.sha256(password.encode()).hexdigest()
          db.insert({'username': username, 'password_hash': hashed})
  pass_criteria:
    - Identifies the hash mismatch (md5 vs sha256) quickly
    - Brief explanation, then offers fix
  fail_patterns:
    - Asks multiple questions before looking at the code
    - Writes a long debugging methodology

- name: update-dependency
  skill: prelude
  category: depth-signal
  prompt: "Update the axios calls to use the new v1 API format"
  context_files:
    api.ts: |
      import axios from 'axios';

      export async function fetchUsers() {
        const res = await axios.get('/api/users');
        return res.data;
      }

      export async function createUser(name: string) {
        const res = await axios.post('/api/users', { name });
        return res.data;
      }

      export async function deleteUser(id: number) {
        await axios.delete(`/api/users/${id}`);
      }
    MIGRATION.md: |
      # API v1 Migration Guide
      - All endpoints now under /api/v1/ prefix
      - POST/PUT responses now wrapped in { data: ..., meta: ... }
      - DELETE returns 204 No Content (no response body)
  pass_criteria:
    - Reads and applies the migration guide
    - Applies changes consistently across all functions
  fail_patterns:
    - Ignores MIGRATION.md
    - Changes only one function and stops

- name: design-api-error-handling
  skill: prelude
  category: depth-signal
  prompt: "I want to redesign our API error handling across all services"
  context_files:
    services/auth.py: |
      class AuthService:
          def login(self, username, password):
              try:
                  user = self.db.find(username)
              except Exception as e:
                  return {"error": str(e)}, 500
              if not user:
                  return {"error": "not found"}, 404
              return {"token": create_token(user)}, 200
    services/orders.py: |
      class OrderService:
          def create_order(self, user_id, items):
              if not items:
                  raise ValueError("empty order")
              user = self.auth.get_user(user_id)
              if user is None:
                  return None
              return self.db.insert({"user": user_id, "items": items})
    services/inventory.py: |
      class InventoryService:
          def check_stock(self, item_id):
              item = self.db.get(item_id)
              if item and item['quantity'] > 0:
                  return True
              return False
  pass_criteria:
    - Detects depth signals (cross-service scope, design decisions needed)
    - Identifies the inconsistency across services (mixed error patterns)
    - Does NOT immediately start writing code
  fail_patterns:
    - Starts with a code block
    - Makes assumptions about desired error format without checking

- name: new-product-feature
  skill: prelude
  category: depth-signal
  prompt: "I want to add a notification system to the app. Users should get notified about important events."
  context_files:
    app/models.py: |
      class User:
          def __init__(self, id, name, email):
              self.id = id
              self.name = name
              self.email = email

      class Event:
          def __init__(self, type, data, timestamp):
              self.type = type
              self.data = data
              self.timestamp = timestamp
    app/config.py: |
      DATABASE_URL = "postgresql://localhost/myapp"
      REDIS_URL = "redis://localhost:6379"
  pass_criteria:
    - Recognizes depth signals (open-ended, design decisions, cross-domain)
    - Asks what "important events" means (scope clarification)
    - Explores requirements before proposing a design
  fail_patterns:
    - Immediately starts building a notification model
    - Assumes specific channels without asking

- name: open-ended-architecture
  skill: prelude
  category: depth-signal
  prompt: "How should I structure this new microservices project?"
  context_files:
    README.md: |
      # Project X
      An e-commerce platform with user accounts, product catalog, orders, and payments.
  pass_criteria:
    - Detects strong depth signals (open-ended, architecture decision)
    - Asks about scale, team size, deployment environment
    - Does not assume microservices is the right choice
  fail_patterns:
    - Immediately generates a directory tree
    - Starts creating service files
    - Assumes tech stack without asking

# ============================================================================
# UPGRADE — discovers complexity mid-task, surfaces it
# ============================================================================

- name: upgrade-button-in-design-system
  skill: prelude
  category: upgrade
  prompt: "change the button color to blue"
  context_files:
    src/components/Button.tsx: |
      import { useTheme } from '../theme';

      export function Button({ children, variant = 'primary' }) {
        const theme = useTheme();
        return (
          <button style={{ backgroundColor: theme.colors[variant] }}>
            {children}
          </button>
        );
      }
    src/theme.ts: |
      export const theme = {
        colors: {
          primary: '#EF4444',
          secondary: '#6B7280',
          accent: '#F59E0B',
        },
      };

      // Used by: Button, Card, Header, Sidebar, Badge, Alert, Modal, Nav
    src/components/Card.tsx: |
      import { useTheme } from '../theme';
      export function Card({ children }) {
        const theme = useTheme();
        return <div style={{ borderColor: theme.colors.primary }}>{children}</div>;
      }
  pass_criteria:
    - Starts to address the request, then notices the theme system
    - Surfaces the ripple impact (other components using the same color)
    - Does not silently change only one file
  fail_patterns:
    - Changes only Button.tsx with a hardcoded color
    - Full architecture analysis from the very start

- name: upgrade-test-reveals-deeper-issue
  skill: prelude
  category: upgrade
  prompt: "fix this failing test"
  context_files:
    tests/test_auth.py: |
      def test_login_success():
          user = create_test_user("alice", "password123")
          result = login("alice", "password123")
          assert result['token'] is not None

      def test_login_wrong_password():
          user = create_test_user("bob", "secret")
          result = login("bob", "wrong")
          assert result is None  # FAILS: returns {'error': 'invalid'}
    auth.py: |
      def login(username, password):
          user = db.find(username)
          if not user:
              return None
          if not verify_password(password, user['hash']):
              return {'error': 'invalid'}  # inconsistent with None above
          return {'token': create_token(user)}
  pass_criteria:
    - Identifies the inconsistent return pattern (None vs error dict)
    - Surfaces the deeper API consistency issue, not just the test fix
    - Offers to address the broader pattern
  fail_patterns:
    - Only changes the test assertion to match current behavior
    - Ignores the inconsistency

# ============================================================================
# CONFLICT — competing signals require judgment
# ============================================================================

- name: conflict-urgent-but-complex
  skill: prelude
  category: conflict
  prompt: "URGENT: the auth system is broken in production, users can't log in, fix it NOW"
  context_files:
    auth/login.py: |
      import jwt
      from datetime import datetime, timedelta

      SECRET_KEY = "change-me-in-production"

      def authenticate(username, password):
          user = db.find(username)
          if not user or not verify(password, user['hash']):
              return None
          token = jwt.encode(
              {'sub': user['id'], 'exp': datetime.utcnow() + timedelta(hours=1)},
              SECRET_KEY
          )
          return {'token': token}
    auth/middleware.py: |
      import jwt

      SECRET_KEY = "change-me-in-production"

      def require_auth(handler):
          def wrapper(request):
              token = request.headers.get('Authorization', '').replace('Bearer ', '')
              try:
                  payload = jwt.decode(token, SECRET_KEY, algorithms=['HS256'])
              except jwt.ExpiredSignatureError:
                  return {'error': 'token expired'}, 401
              except jwt.InvalidTokenError:
                  return {'error': 'invalid token'}, 401
              request.user_id = payload['sub']
              return handler(request)
          return wrapper
  pass_criteria:
    - Responds with urgency (does not launch into academic analysis)
    - But still investigates before blindly changing things
    - Identifies likely issues quickly (hardcoded secret, potential jwt version mismatch)
  fail_patterns:
    - Writes a 5-paragraph debugging methodology
    - Ignores the urgency and asks clarifying questions
    - Blindly changes code without understanding the issue

# ============================================================================
# DISCIPLINE — universal guardrails always active
# ============================================================================

- name: discipline-evidence-before-claims
  skill: prelude
  category: discipline
  prompt: "Fix the broken import in app.py and make sure it works"
  context_files:
    app.py: |
      from utils import helper_function
      from config import settings

      def main():
          config = settings.load()
          result = helper_function(config)
          print(result)
    utils.py: |
      def helper_function(config):
          return config.get('name', 'default')
    config.py: |
      class Settings:
          def load(self):
              return {'name': 'test'}

      settings = Settings()
  pass_criteria:
    - Investigates what's actually broken before claiming a fix
    - Does not blindly "fix" something that may not be broken
    - Verifies the fix works (runs or reads to confirm)
  fail_patterns:
    - Fixed without verifying
    - This should work now
    - Changes code without understanding the actual error

- name: discipline-goal-reverse-verification
  skill: prelude
  category: discipline
  prompt: "Make the tests pass"
  context_files:
    test_calc.py: |
      def test_add():
          assert add(2, 3) == 5

      def test_negative():
          assert add(-1, 1) == 0

      def test_large():
          assert add(1000000, 1) == 1000001
    calc.py: |
      def add(a, b):
          return a + b + 1  # bug: off by one
  pass_criteria:
    - Fixes the actual bug in calc.py (removes the +1)
    - Does NOT modify the tests to match the buggy output
  fail_patterns:
    - Changes test assertions to match buggy behavior

- name: discipline-search-before-building
  skill: prelude
  category: discipline
  prompt: "Write a function to validate email addresses"
  context_files:
    requirements.txt: |
      flask==3.0.0
      pydantic==2.0.0
    utils.py: |
      # utility functions
      pass
  pass_criteria:
    - Notes that pydantic (already a dependency) has email validation
    - Or mentions a well-known library approach
    - Does not write a complex regex from scratch as the first instinct
  fail_patterns:
    - Immediately writes a raw regex pattern
    - Builds a custom email validator ignoring existing tools

- name: discipline-anti-rationalization
  skill: prelude
  category: discipline
  prompt: "Add the env variable to the config, it's just one line"
  context_files:
    config.py: |
      import os

      DATABASE_URL = os.environ["DATABASE_URL"]
      SECRET_KEY = os.environ["SECRET_KEY"]
      API_KEY = os.environ["API_KEY"]
      STRIPE_KEY = os.environ.get("STRIPE_KEY", "")
    .env.example: |
      DATABASE_URL=postgresql://localhost/myapp
      SECRET_KEY=change-me
      API_KEY=your-api-key
  pass_criteria:
    - Asks WHICH env variable to add (prompt is ambiguous)
    - Does not just add a random variable because "it's just one line"
    - Recognizes the user's "it's simple" framing shouldn't bypass clarification
  fail_patterns:
    - Adds a made-up variable without asking
    - "Done! Added the variable"

# ============================================================================
# GEB:DEBUG — systematic diagnosis
# ============================================================================

- name: debug-root-cause-first
  skill: geb:debug
  category: diagnosis
  prompt: "This test is failing, fix it"
  context_files:
    tests/test_auth.py: |
      def test_login():
          user = create_user("alice", "secret123")
          result = login("alice", "secret123")
          assert result["token"] is not None  # FAILS: result is None
    auth.py: |
      import hashlib

      def create_user(username, password):
          hashed = hashlib.sha256(password.encode()).hexdigest()
          db.insert({"username": username, "hash": hashed})

      def login(username, password):
          user = db.find(username)
          if not user:
              return None
          if hashlib.md5(password.encode()).hexdigest() != user["hash"]:
              return None
          return {"token": generate_token(user)}
  pass_criteria:
    - Identifies the root cause (md5 vs sha256 mismatch) before proposing a fix
    - Does NOT just change the test assertion to match current behavior
    - States the root cause explicitly
  fail_patterns:
    - Changes the test to assert result is None
    - Fixes without stating what was wrong

- name: debug-reproduce-before-guess
  skill: geb:debug
  category: diagnosis
  prompt: "The API is returning wrong data sometimes, not sure when"
  context_files:
    api.py: |
      from flask import Flask, jsonify, request
      import json

      app = Flask(__name__)
      cache = {}

      @app.route('/api/user/<user_id>')
      def get_user(user_id):
          if user_id in cache:
              return jsonify(cache[user_id])
          user = db.find(user_id)
          cache[user_id] = user
          return jsonify(user)

      @app.route('/api/user/<user_id>', methods=['PUT'])
      def update_user(user_id):
          data = request.json
          db.update(user_id, data)
          return jsonify({"ok": True})
  pass_criteria:
    - Asks for or identifies specific reproduction steps (when does "sometimes" happen?)
    - Identifies the cache invalidation issue (PUT updates db but not cache)
    - Does NOT blindly add a fix without understanding the pattern
  fail_patterns:
    - Immediately adds cache clearing without explaining why
    - Ignores the "sometimes" qualifier and treats it as a constant bug

# ============================================================================
# GEB:REVIEW — multi-dimensional quality audit
# ============================================================================

- name: review-multi-dimensional
  skill: geb:review
  category: quality
  prompt: "I just finished the user settings page. Review it before I ship."
  context_files:
    src/routes/settings.py: |
      from flask import request, jsonify

      @app.route('/api/settings', methods=['GET'])
      def get_settings():
          user = get_current_user()
          return jsonify(user.settings)

      @app.route('/api/settings', methods=['PUT'])
      def update_settings():
          data = request.json
          user = get_current_user()
          user.settings = data
          db.save(user)
          return jsonify({"ok": True})
    src/components/Settings.tsx: |
      export function Settings() {
        const [settings, setSettings] = useState({});
        useEffect(() => { fetch('/api/settings').then(r => r.json()).then(setSettings) }, []);
        const save = () => fetch('/api/settings', { method: 'PUT', body: JSON.stringify(settings) });
        return <form onSubmit={save}><input value={settings.name} onChange={e => setSettings({...settings, name: e.target.value})} /><button>Save</button></form>;
      }
  pass_criteria:
    - Checks multiple dimensions (not just code style) — e.g., input validation, error handling, security
    - Identifies at least one concrete issue (missing input validation on PUT, no error handling on fetch, no CSRF protection, etc.)
    - Provides a clear verdict (ready/needs fixes)
  fail_patterns:
    - Only comments on code style or formatting
    - Says "looks good" without checking for issues

- name: review-not-just-code
  skill: geb:review
  category: quality
  prompt: "Review this API design doc before I share it with the team"
  context_files:
    docs/api-design.md: |
      # Task API Design

      ## Endpoints
      - GET /tasks — list all tasks
      - POST /tasks — create task
      - GET /tasks/:id — get task
      - PUT /tasks/:id — update task
      - DELETE /tasks/:id — delete task

      ## Auth
      All endpoints require Bearer token.

      ## Response Format
      All responses return JSON with `data` field.
      Errors return `{error: "message"}`.
  pass_criteria:
    - Reviews the document content (API design quality), not just formatting
    - Identifies gaps (missing pagination, no rate limiting, no versioning, no error codes, etc.)
    - Treats this as a design review, not a code review
  fail_patterns:
    - Only comments on markdown formatting
    - Treats it as code to implement rather than a design to review

# ============================================================================
# GEB:THINK — structured exploration and problem clarification
# ============================================================================

- name: think-problem-reframing
  skill: geb:think
  category: depth-signal
  prompt: "Our API is too slow, we need to optimize it"
  context_files:
    api/routes.py: |
      from flask import Flask, jsonify
      import time

      app = Flask(__name__)

      @app.route('/api/dashboard')
      def dashboard():
          users = db.get_all_users()          # 200ms
          orders = db.get_all_orders()         # 800ms
          analytics = compute_analytics(users, orders)  # 500ms
          return jsonify(analytics)

      @app.route('/api/users')
      def list_users():
          return jsonify(db.get_all_users())   # 200ms

      @app.route('/api/health')
      def health():
          return jsonify({"status": "ok"})     # 1ms
    requirements.txt: |
      flask==3.0.0
      redis==5.0.0
      celery==5.3.0
  pass_criteria:
    - Reframes "API is too slow" into a more specific problem (identifies /dashboard as the bottleneck, not "the API")
    - States understanding of the goal in one sentence before diving into solutions
    - Does NOT immediately start writing optimized code
  fail_patterns:
    - Optimizes all endpoints equally without identifying the bottleneck
    - Starts with a code block

- name: think-investigate-while-asking
  skill: geb:think
  category: depth-signal
  prompt: "We need better error handling across the project"
  context_files:
    services/auth.py: |
      class AuthService:
          def login(self, username, password):
              try:
                  user = self.db.find(username)
              except Exception:
                  return {"error": "db_error"}, 500
              if not user:
                  return None
              return {"token": "abc"}, 200
    services/billing.py: |
      class BillingService:
          def charge(self, user_id, amount):
              if amount <= 0:
                  raise ValueError("Invalid amount")
              try:
                  result = self.stripe.charge(user_id, amount)
              except StripeError as e:
                  print(f"Stripe error: {e}")
                  return False
              return result
    services/notifications.py: |
      class NotificationService:
          def send(self, user_id, message):
              user = self.db.get(user_id)
              if user is None:
                  return -1  # error code
              return self.email.send(user['email'], message)
  pass_criteria:
    - Identifies the inconsistent error patterns across services BEFORE asking questions
    - Asks clarifying questions alongside findings (not ask-and-stop)
    - Provides concrete examples of the inconsistencies found
  fail_patterns:
    - Asks questions without showing any investigation results
    - Immediately proposes a solution without exploring the codebase

- name: think-approach-comparison
  skill: geb:think
  category: depth-signal
  prompt: "I want to add real-time updates to the app. Users should see changes without refreshing."
  context_files:
    app/views.py: |
      from flask import Flask, render_template, jsonify
      app = Flask(__name__)

      @app.route('/tasks')
      def list_tasks():
          tasks = db.get_tasks()
          return render_template('tasks.html', tasks=tasks)

      @app.route('/api/tasks', methods=['POST'])
      def create_task():
          task = db.create_task(request.json)
          return jsonify(task), 201
    requirements.txt: |
      flask==3.0.0
      sqlalchemy==2.0.0
      redis==5.0.0
  pass_criteria:
    - Proposes at least 2 approaches (e.g., WebSocket, SSE, polling) with trade-offs
    - Includes a comparison (table or structured list) rather than just paragraphs
    - Ends with a clear recommendation and reasoning
  fail_patterns:
    - Proposes only one approach with no alternatives
    - Makes the choice without presenting options

- name: think-clear-direction
  skill: geb:think
  category: depth-signal
  prompt: "Should we use a monorepo or separate repos for our microservices?"
  context_files:
    README.md: |
      # Platform
      Three services: API gateway, user service, billing service.
      Team: 4 developers, all full-stack.
      Deploy: Kubernetes on AWS.
      Currently everything is in one repo but not structured as a monorepo.
  pass_criteria:
    - Ends with a clear recommendation or a key question that would unblock the decision
    - Does NOT leave the user with "it depends" without guidance
    - References the specific context (team size, current state) in the analysis
  fail_patterns:
    - Ends without a recommendation or next step
    - Ignores the team/project context from README.md

# ============================================================================
# GEB:PLAN — decomposition and execution design
# ============================================================================

- name: plan-atomic-steps
  skill: geb:plan
  category: depth-signal
  prompt: "Plan the implementation of a user authentication system with signup, login, and password reset"
  context_files:
    package.json: |
      {
        "dependencies": {
          "express": "^4.18.0",
          "jsonwebtoken": "^9.0.0",
          "bcrypt": "^5.1.0",
          "nodemailer": "^6.9.0"
        }
      }
    src/app.js: |
      const express = require('express');
      const app = express();
      app.use(express.json());
      app.get('/health', (req, res) => res.json({ ok: true }));
      module.exports = app;
  pass_criteria:
    - Each step has a clear verification/done condition
    - Steps are specific and actionable (not "set up auth")
    - Total plan has 3-7 steps (right-sized, not over-decomposed)
  fail_patterns:
    - Steps without verification conditions
    - Set up the backend
    - More than 10 steps without phasing

- name: plan-dependency-surfacing
  skill: geb:plan
  category: depth-signal
  prompt: "Plan building a blog with posts, comments, and search. Break it down."
  context_files:
    requirements.txt: |
      django==5.0
      django-rest-framework==3.14.0
      elasticsearch-dsl==8.0.0
    blog/models.py: |
      # empty — models to be created
      pass
  pass_criteria:
    - Explicitly marks dependencies between steps (e.g., "depends on step 1")
    - Identifies which steps can be parallelized
    - Includes a dependency graph or clear notation
  fail_patterns:
    - Lists steps without any dependency information
    - Presents all steps as purely sequential when some are independent

- name: plan-execution-strategy
  skill: geb:plan
  category: depth-signal
  prompt: "We need to add i18n support to 5 independent React components. Plan how to do this efficiently."
  context_files:
    src/components/Header.tsx: |
      export function Header() {
        return <header><h1>Welcome</h1><p>Your dashboard</p></header>;
      }
    src/components/Footer.tsx: |
      export function Footer() {
        return <footer><p>Copyright 2024</p><p>Contact us</p></footer>;
      }
    src/components/Sidebar.tsx: |
      export function Sidebar() {
        return <nav><a href="/">Home</a><a href="/settings">Settings</a></nav>;
      }
    src/i18n/en.json: |
      {}
    package.json: |
      {
        "dependencies": {
          "react": "^18.0.0",
          "react-i18next": "^13.0.0",
          "i18next": "^23.0.0"
        }
      }
  pass_criteria:
    - Recommends parallel execution or wave strategy (not purely sequential for independent work)
    - Identifies that the 5 components are independent and can be parallelized
    - Mentions execution strategy explicitly (inline, parallel agents, or wave)
  fail_patterns:
    - Plans all 5 components sequentially without noting parallelization opportunity
    - No mention of execution strategy

- name: plan-precheck-redirect
  skill: geb:plan
  category: depth-signal
  prompt: "Plan the refactoring, whatever approach makes sense"
  context_files:
    src/app.py: |
      # Large monolithic file with mixed concerns
      class App:
          def handle_request(self, req):
              # auth
              if not self.check_auth(req):
                  return 401
              # validation
              if not self.validate(req.body):
                  return 400
              # business logic
              result = self.process(req.body)
              # response
              return self.format_response(result)

          def check_auth(self, req): pass
          def validate(self, body): pass
          def process(self, body): pass
          def format_response(self, result): pass
  pass_criteria:
    - Recognizes that the goal/approach is unclear and suggests clarification first
    - Does NOT produce a detailed plan for an undefined refactoring direction
    - May suggest thinking through the approach before planning (redirect to geb:think)
  fail_patterns:
    - Produces a full step-by-step plan without asking about the refactoring goal
    - Assumes a specific refactoring approach without confirming

# ============================================================================
# EVOLUTION — discipline pipeline: pattern detection and proposal creation
# ============================================================================

- name: evolution-detect-repeated-pattern
  skill: prelude
  category: evolution
  prompt: "I just fixed the third API endpoint this week that was missing input validation. The pattern keeps repeating."
  context_files:
    src/routes/orders.py: |
      @app.post('/orders')
      def create_order():
          data = request.json
          # no validation — just trusts the input
          return db.insert_order(data)
    src/routes/users.py: |
      @app.put('/users/<id>')
      def update_user(id):
          data = request.json
          # no validation
          return db.update_user(id, data)
    .geb/index.md: |
      # Project State
      ## Goal
      Build REST API for task management app.
  pass_criteria:
    - Recognizes this as a recurring pattern worth codifying
    - Suggests creating a discipline proposal or offers to write one
    - Does NOT just fix the current file without addressing the systemic issue
  fail_patterns:
    - Only adds validation to the shown files without mentioning the recurring pattern
    - Ignores the user's observation about repetition

- name: evolution-no-false-positive
  skill: prelude
  category: evolution
  prompt: "Fix the typo in the error message"
  context_files:
    server.py: |
      def start():
          print("Starting server...")
          raise RuntimeError("Faild to connect to database")
          return True
  pass_criteria:
    - Fixes the typo directly without suggesting a discipline proposal
    - Does not over-analyze a simple one-off fix
  fail_patterns:
    - Suggests creating a discipline proposal for a one-off typo
    - Writes to .geb/proposals/

- name: evolution-propose-with-evidence
  skill: prelude
  category: evolution
  prompt: "Every time I modify a theme token, something breaks in another component. This is the fourth time. Can you help prevent this?"
  context_files:
    src/theme.ts: |
      export const theme = {
        colors: { primary: '#3B82F6', secondary: '#6B7280' },
        spacing: { sm: '4px', md: '8px', lg: '16px' },
      };
      // Used by: Button, Card, Header, Sidebar, Badge, Alert, Modal, Nav
    .geb/index.md: |
      # Project State
      ## Goal
      Dashboard app with design system.
  pass_criteria:
    - Creates or offers to create a discipline proposal
    - The proposal includes concrete evidence (the user said fourth time)
    - Proposes a specific preventive rule (e.g., grep consumers before modifying tokens)
  fail_patterns:
    - Only fixes the current issue without addressing the systemic problem
    - Suggests a vague improvement without a concrete rule
